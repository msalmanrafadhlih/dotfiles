{

  catppuccin.thunderbird = {
    enable = true;
    flavor = "mocha";
    accent = "lavender";
  };

  # programs.thunderbird.enable = true; already enabled in programs.nix (system)

  services.protonmail-bridge.enable = true;
}
